<h1><%= t('Editing %<name>s') % {name: Dojo.model_name.human} %></h1>

<%= render 'form' %>

<p><%= link_to t('Destroy'), @dojo, confirm: t('Are you sure?'), method: :delete %></p>
<p><%= link_to 'Back', :root %></p>
